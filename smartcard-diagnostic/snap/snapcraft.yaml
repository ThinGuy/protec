name: smartcard-diagnostic
base: core24
version: '2.0.0'
summary: Smart Card Diagnostic Tool
description: |
  Professional diagnostic and monitoring tool for government and enterprise
  smart card deployments. Supports PIV cards and enterprise PKI systems.

  Features:
  - Real-time card/reader detection
  - Certificate validation
  - FIPS compliance checking
  - Support bundle generation

  Designed for Ubuntu Desktop and Ubuntu Core.

grade: stable
confinement: strict

platforms:
  amd64:
  arm64:

apps:
  smartcard-diagnostic:
    command: bin/smartcard-diagnostic
    plugs:
      - desktop
      - desktop-legacy
      - wayland
      - x11
      - home
      - removable-media
      - hardware-observe
      - network
      - network-bind
      - raw-usb
      - opengl
    environment:
      LD_LIBRARY_PATH: $SNAP/usr/lib/x86_64-linux-gnu:$SNAP/usr/lib
      PATH: $SNAP/usr/bin:$SNAP/usr/sbin:$PATH

  monitor:
    command: bin/smartcard-monitor
    daemon: simple
    restart-condition: always
    plugs:
      - hardware-observe
      - raw-usb
    slots:
      - dbus-smartcard

slots:
  dbus-smartcard:
    interface: dbus
    bus: session
    name: com.canonical.SmartCardMonitor

parts:
  smartcard-diagnostic:
    plugin: flutter
    source: .
    flutter-target: lib/main.dart
    build-packages:
      - clang
      - cmake
      - ninja-build
      - libgtk-3-dev
      - pkg-config
    stage-packages:
      - libgtk-3-0
      - libgdk-pixbuf2.0-0
      - libglib2.0-0
    override-build: |
      craftctl default
      mkdir -p $CRAFT_PART_INSTALL/bin
      cp -r build/linux/*/release/bundle/* $CRAFT_PART_INSTALL/bin/
      mv $CRAFT_PART_INSTALL/bin/smartcard_diagnostic $CRAFT_PART_INSTALL/bin/smartcard-diagnostic

  monitor-daemon:
    plugin: dump
    source: daemon/
    stage-packages:
      - python3
      - python3-dbus
      - python3-gi
      - gir1.2-glib-2.0
    override-build: |
      craftctl default
      mkdir -p $CRAFT_PART_INSTALL/bin
      cp smartcard_monitor.py $CRAFT_PART_INSTALL/bin/smartcard-monitor
      chmod +x $CRAFT_PART_INSTALL/bin/smartcard-monitor

  system-dependencies:
    plugin: nil
    stage-packages:
      - opensc
      - pcscd
      - libpam-pkcs11
      - opensc-pkcs11
      - gnutls-bin
      - libccid
      - pcsc-tools
      - libpcsclite1
